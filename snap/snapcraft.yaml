name: freeciv-snap # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '3.0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: Single-line elevator pitch for your amazing snap # 79 char long summary
description: |
  This is my-snap's description. You have a paragraph or two to tell the
  most important story about your snap. Keep it under 100 words though,
  we live in tweetspace and your description wants to look good in the snap
  store.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  freeciv:
    plugin: autotools
    source: http://files.freeciv.org/stable/freeciv-3.0.1.tar.xz
    autotools-configure-parameters:
      - --disable-dependency-tracking
      - --enable-fcdb=sqlite3
      - --enable-ipv6=yes
      - --enable-patient-connect=yes
      - --enable-mapimg=magickwand
      - --enable-json
      - --enable-client=gtk2,gtk3.22,qt
      - --enable-gitrev
      - --enable-debug=no
      - --enable-sdl-mixer=sdl2
      - --enable-fcmp=gtk3,qt
      - --enable-sys-lua
      - --with-readline
      - --with-libbz2
      - --with-liblzma
      - CXXFLAGS=-Os
